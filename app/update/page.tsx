"use client";
//export const metadata = { title: 'Update' };
import { Switch } from '@heroui/react';

export default function Update() {
  return (
    
      <Switch defaultSelected aria-label="Dark theme" />
      
  );
}
